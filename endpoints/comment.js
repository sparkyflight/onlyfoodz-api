const crypto = require("node:crypto"); 
  
module.exports = { 
         name: "posts/comment", 
         method: "POST", 
         execute: async (req, res, database, Spotify) => {
           const data = req.body;
         }
};
